import React from "react";

const Settings: React.FC = () => (
  <div className="page settings">
    <h2>Settings</h2>
    <p>Configure default AI models, enhancement methods, and storage destinations.</p>
  </div>
);

export default Settings;
